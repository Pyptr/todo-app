# 待办清单 App · 设计规格（开发交接）

> 来源设计稿：`待办清单App设计`（.ardot，画布 fileId `709945910012734`）
> 平台：Android 移动端 · 视觉风格：暖奶油底 + 鼠尾草绿（Soft Nordic / Things 3 质感）
> 单屏基准尺寸：390 × 844 px

---

## 1. 设计 Token

### 1.1 配色（Color）
| 用途 | 名称 | Hex | 备注 |
|---|---|---|---|
| 页面背景 | Background | `#F8F6F2` | 暖奶油色，全屏底色 |
| 卡片背景 | Card | `#FFFFFF` | 列表项 / 卡片 |
| 文字主色 | Text Primary | `#1A1A1A` | 标题、正文重点 |
| 文字次色 | Text Secondary | `#5C5C5C` | 标签、说明 |
| 文字弱色 | Text Muted | `#9A9A9A` | 占位、辅助、未选中 |
| 占位符 | Placeholder | `#BDBDBD` | 输入框提示文字 |
| 描边·细 | Border Subtle | `#E8EDEB` | 输入框、列表分割 |
| 描边·中 | Border | `#D4DCD8` | 未勾选框描边 |
| 主色 | Accent | `#6BA8A0` | 鼠尾草绿，按钮/进度/选中 |
| 主色·深 | Accent Dark | `#3E8278` | 热力图最高强度 |
| 主色·浅底 | Accent Tint | `#EAF4F2` | 选中行、标签底、铃声选中 |
| 进度轨道 | Track | `#EDEAE4` | 进度条底槽 |

**热力图 5 级强度**（由低到高）：
`#E8EDEB` → `#C7DED8` → `#9CC4BB` → `#6BA8A0` → `#3E8278`

### 1.2 字体（Typography）
- 中文：`Noto Sans SC`（Regular / Medium / SemiBold / Bold）
- 数字 · 时间：`Inter`（Regular / Medium / SemiBold / Bold）
- 字号阶梯：

| 角色 | 字号 | 字重 | 示例 |
|---|---|---|---|
| 大数字（统计） | 32 | Bold | 连续 7 天 |
| 页面大标题 | 28 | SemiBold | 待办清单 / 打卡统计 |
| 屏幕标题 | 20 | SemiBold | 新建待办 / 提醒设置 |
| 正文 / 输入值 | 15–16 | Regular / SemiBold | 事项名、进度百分比 |
| 次要说明 | 13–14 | Regular / Medium | 子标题、标签 |
| 极小标 | 10–12 | Medium | 导航标签、热力图图例 |

### 1.3 圆角（Radius）
- 卡片 / 大容器：16
- 输入框 / 列表行 / 主按钮：12
- 标签（pill）：11
- 进度条：3–4
- 开关：14（轨道）/ 11（旋钮）
- 底部胶囊导航：36（外）/ 26（内 tab）

### 1.4 间距与阴影（Spacing & Shadow）
- 基础栅格：8px；屏幕左右内边距：20；区块间距：16 / 24
- 卡片内边距：16；列表项间距：12
- 卡片阴影：`0 2px 10px rgba(107,168,160,0.10)`（鼠尾草绿低透明柔影）
- 胶囊导航容器底部安全区内边距：bottom 21 / left·right 21 / top 12

---

## 2. 通用组件规格

| 组件 | 尺寸 | 样式要点 |
|---|---|---|
| 状态栏 Status Bar | 高 62 | 时间 `9:41`（Inter SemiBold 15）左，信号/电池指示右 |
| 底部胶囊导航 Tab Bar | 高 ~62 + 安全区 | 4 个 tab；选中态实填主色 `#6BA8A0`（白字白图标），未选中 `#9A9A9A` |
| 勾选框 Checkbox | 24×24，圆角 6 | 未选：白底 + `#D4DCD8` 描边；选中：主色填充 + 白色对勾 |
| 开关 Toggle | 48×28，圆角 14 | 开启：主色底 + 22 白旋钮靠右；关闭：灰底 + 旋钮靠左 |
| 进度条 Progress | 高 6–8，圆角 3–4 | 底槽 `#EDEAE4`，填充按百分比着色主色 |
| 列表项卡片 | 圆角 16，padding 16 | 左勾选框 + 中（标题/进度文案/进度条）+ 右铃铛 |
| 输入框 Input | 高 56，圆角 12 | 白底 + `#E8EDEB` 描边，左对齐文字 |
| 图标按钮 | 40–44 圆形 | 白底 + 柔影，内置 24 线性图标 |
| 主按钮 Button | 高 54，圆角 12 | 主色填充，白字 SemiBold 16 |

---

## 3. 各页面规格

### 3.1 待办列表首页（Screen 1）
- Header：左「周六 · 8月1日 / 待办清单」+ 右「+」添加按钮（44 圆，主色）
- 今日打卡进度卡：标签「今日打卡进度」+ 百分比「35%」（Inter Bold 22）+ 进度条（填充 35%）+ 副文案「已勾选 3 / 9 项」
- 进行中的事项：列表项卡片
  - 示例数据：阅读 30 分钟（12/100 · 第12天）、健身打卡（45/100 · 第45天，已勾选）、背单词 50 个（1/100 · 第1天）
  - 每项含勾选框、标题、进度文案、进度条、提醒铃铛（设置提醒时显示）
- 底部导航：待办（选中）/ 提醒 / 统计 / 我的

### 3.2 新建 / 编辑事项（Screen 2）
- Header：返回箭头 + 标题「新建待办」+ 右侧「保存」
- 表单字段：
  - 事项名称：输入框，占位「例如：阅读 30 分钟」
  - 总天数 / 已打卡天数：两列并排输入（默认 100 / 1）
  - 进度预览：实时显示「进度预览 X%」+ 进度条
  - 开关行：「勾选后计入今日完成」（默认开）
  - 提醒设置入口：右箭头跳转 Screen 3
- 底部「保存待办」主按钮（主色）

### 3.3 提醒设置（Screen 3）
- Header：返回 + 标题「提醒设置」+ 保存
- 总开关：「开启提醒」
- 定时提醒卡片：
  - 标签「定时提醒」+ 标签 pill「精确到秒」（主色浅底）
  - 时间选择：时 / 分 / 秒 三格（72×60，圆角12），默认 `14 : 00 : 23`
  - 重复：每天（可展开）
- 间隔提醒卡片：
  - 说明「从设定时间起，每隔一段时间提醒你」
  - 开始时间 `14:00`
  - 间隔 `15` + 单位切换「分钟 / 小时」（分钟选中）
- 提醒铃声入口：右箭头跳转 Screen 4
- 底部「保存设置」主按钮

### 3.4 提醒铃声（Screen 4）
- Header：返回 + 标题「提醒铃声」+ 保存
- 当前选中预览卡：播放按钮（主色圆）+ 名称「水滴声」+ 副「当前选中」
- 系统铃声：单选列表（水滴声 / 鸟鸣 / 清脆 / 柔和），选中行主色浅底 + 主色实心圆点
- 在线搜歌：搜索输入框（放大镜图标 + 占位「搜索歌曲名称，例如 River Flows in You」）
  - 结果行：音符图标 + 曲名（如 River Flows in You / 晴天 - 周杰伦）+ 「下载」按钮
- 底部「使用选中铃声」主按钮

### 3.5 打卡统计（Screen 5）
- Header：标题「打卡统计」
- 统计卡：连续打卡「7」天 / 本月打卡「18」天（白卡 + 主色大数字 32）
- GitHub 风格热力图：
  - 标题「近 10 周打卡热度」
  - 左侧日标签（一/三/五/日），右侧 10 列 × 7 行 = 70 格（14×14，圆角3）
  - 着色按 5 级强度规则
  - 图例：少 → 多（5 个色块）
- 底部导航：待办 / 提醒 / 统计（选中）/ 我的

---

## 4. 关键交互规则（开发要点）

1. **进度计算**：`进度% = 已打卡天数 / 总天数`，如 1/100 = 1%。进度条填充宽度按百分比映射。
2. **今日完成判定**：仅当用户勾选该事项，且开关「勾选后计入今日完成」开启时，才计入当日打卡；否则当日不计数。
3. **定时提醒**：支持精确到秒（HH:MM:SS），如 `14:00:23`；可设置重复（每天/自定义）。
4. **间隔提醒**：从「开始时间」起，按所选「间隔值 + 单位（分钟/小时）」循环触发。
5. **提醒铃声**：可本地选择系统铃声；也可通过在线 API 搜索歌曲并下载到本地作为铃声。API 需开源免费、无需 token（如确需 token 应支持一键自动申请并注入）。
6. **热力图着色**：按当日打卡强度映射 5 级色阶；统计卡「连续打卡」「本月打卡」由勾选数据聚合得出。

---

## 5. 图标清单（线性 SVG，描边 1.8–2.2）
- 添加 `+`、返回 `‹`、搜索 `🔍`、铃铛、对勾 `✓`、播放、音符、下载、柱状图（统计）、人像（我的）、列表（待办）、右箭头 `›`、信号/电池（状态栏）。
- 规范：图标一律用 SVG 节点（禁止 emoji 当图标）；未选中 `#9A9A9A`，主色相关 `#6BA8A0`，选中态白色。

---

## 6. 交付说明
- 本规格对应高保真原型，所有尺寸/色值均按设计稿标注，可直接用于 Android（XML / Compose）实现。
- 实现层（搜歌 API 接入、Android 编译打包）不在设计范围，需由工程侧基于以上交互规则落地。
